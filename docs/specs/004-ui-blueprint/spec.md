# ui-blueprint — 规格

状态：Draft（草案）

日期：2026-09-02

相关：ADR 004、ADR 006；`docs/specs/001-point-cloud-viewport/`、`docs/specs/002-display-types/`、`docs/specs/003-i18n-system-fonts/`（已闭环，本功能为布局层演进，脚本与文案体系沿用）

> 跨 workspace 引用全限定（docs/README 约定）；裸编号仅本 workspace 有效。

## 1. 问题陈述

现有界面是过程演进而非设计的结果：左栏平铺列表（无分组聚合/右键/搜索）、主操作散在菜单（无工具条）、
Add frame/marker 是浮动非模态窗口（与"树+属性即编辑"的成熟工具范式相悖）、无选中语义、无底部状态栏、
无工程色板。**本功能按成熟 3D 工具共识建立四区信息架构**（左对象树/中视口/右属性/底状态）——这是后续
拾取（005）、停靠布局（006）、交互打磨（007）的地基。

**取代声明**：本功能将 002-display-types 的"固定单列侧栏 + 浮动 Add 窗口"布局升级为四区骨架；
侧栏功能迁移至左对象树（树仍为固定面板，直至 006 引入停靠）；Add frame/marker 的浮动 Window
形态**取消**，改为工具栏"添加"inline 入口。002 布局类验收行按语义等价执行，002 修订行登记取代关系。

## 2. 成功指标（可测试）

| # | 指标 | 判定 |
|---|---|---|
| M1 | 四区骨架 | 左（对象树）/中（3D 视口）/右（属性面板）/底（状态栏）同时存在且各司其职；位置固定 |
| M2 | 树上选中联动 | 树行点击 → 选中态（≤1 帧）→ 右属性面板显示该对象；视口选中高亮同步（高亮方式见 §6） |
| M3 | 无浮动 Add | 全应用无 Add frame/marker 浮动窗口；工具栏"添加▾"内联输入即加（对话框生命周期移除） |
| M4 | 树聚合 | 树按显示类型分组（点云/网格/路径/坐标系/标记组）；组级默认值（颜色）可设并作用于组内新成员 |
| M5 | 状态栏 | 底栏常驻显示：加载状态/帧率/鼠标世界坐标/当前工具提示（至少三项） |
| M6 | 回归 | 001–003 全量验收不受影响（En locale 语义等价；002 被取代行除外）；A9 守卫/五门禁绿 |

## 3. 用户故事

- **US1**：我打开应用即看到标准四区；左树按类型分组显示场景，眼睛切换显隐，组可折叠。
- **US2**：我点击树上某对象 → 右侧属性面板显示其参数，改参数即生效。
- **US3**：我点工具栏"添加▾"→ 出现内联小表单（输入即加），不再有弹窗。
- **US4**：我在视口移动鼠标 → 底栏实时显示世界坐标与当前工具提示；打开文件时底栏显示加载状态。

## 4. 验收标准（渠道：CI=自动，UT=单测，MAN=手工）

| # | 验收 | 渠道 |
|---|---|---|
| A1 | 启动后四区同时可见（左/中/右/底），位置符合 §6 骨架图 | MAN |
| A2 | 树行点击 → 选中高亮（树上行 + 视口同步）≤1 帧；再点他行切换；点空处取消 | MAN |
| A3 | 选中对象后右面板显示其**当前**属性（名称/显隐/类型专用字段：网格→颜色、标记→文本、frame→长度） | MAN |
| A4 | 属性面板修改即生效（≤1 帧），与视口/树同步 | MAN |
| A5 | 全应用无 Add 浮动窗口；"添加▾"内联实现 frame/marker 创建（默认值+回车即加） | MAN |
| A6 | 树按类型分组，组折叠/展开；组级颜色默认值：组内新建对象继承 | MAN |
| A7 | 底栏 ≥3 项常驻信息实时更新（加载状态/帧率/坐标） | MAN |
| A8 | 树上右键菜单：重命名/显隐/删除至少三项可用（改名即时反映树/属性/场景） | MAN |
| A9 | 语义色板：选中高亮色、网格色、坐标轴 RGB（沿用）为统一定义 token | UT(定义断言)+MAN |
| A10 | 回归：001–003 全量（En）+ A9 守卫 + 五门禁/三平台/msrv | CI+MAN |

## 5. 非目标（本功能不做）

- 不做 3D 拾取/选中高亮的射线命中（005-picking-selection 承接；本功能选中=树上驱动）。
- 不做面板停靠/浮动/布局持久化（006-dock-layout）。
- 不做快捷键体系/右键菜单全量/消息中心完整迁移（007-interaction-polish；本功能仅做 A8 树上右键三项）。
- 不做多视口、2D 视图、时间线 scrub、图标库（另立项）。
- 不修改 core 数据模型（树/属性全部经现有 scene API；组默认值为 app 层状态）。
- 不做拖放打开/多选打开（属打开体验小功能，另行评估）。
- **范围锁定**：调整（如分区变化、新增第三面板）须修订本规格并重新批准。

## 6. 约束

- **骨架图**（固定布局期——停靠前）：顶部菜单+工具条（工具条：打开▾/Fit/添加▾/轴/网格/拾取(占位禁用)，
  互斥模式感按钮组）；左=对象树（`objects_panel` 升级：分组/折叠/眼睛/右键/搜索）；中=视口
  （保留，追加 HUD 悬浮层：网格/轴开关——悬浮层为**视口角标**而非菜单）；右=属性面板（新
  `properties_panel.rs`，分组卡片 + DragValue 控件）；底=状态栏（新 `status_bar.rs`）。
- **语义色板**（`ui/theme.rs` 统一定义 token）：选中高亮（橙系）、网格线、原点轴（RGB 沿用）、
  HUD 文字、面板背景（沿用暗色系）；视口底中性灰。
- 选中态：树行/视口/属性三区同一选中对象（id 语义，`Scene` 已有）；无拾取时选中仅来源于树。
- 文案：全部走 003 的 key 表（新键入表、双语）；A9 守卫照旧。
- 组件约束：`egui::SidePanel/TopBottomPanel` 固定分区（egui_dock 属 006）；禁用临时 Window 表达
  主流程交互（错误消息窗口沿用 003 机制不变，直至 007 迁移）。

## 7. 决策点（待人工确认；批准时落位正文并删除本节点）

- D1 选中来源先树后拾取（推荐：本功能树上选中驱动属性；005 再打通视口射线）——确认？
- D2 底部范围：状态栏（帧率/坐标/提示）+ 轻量消息条（错误/警告时间戳内联，错误窗保留并存）——
  还是仅状态栏、消息中心推到 007？（推荐前者）
- D3 工具条按钮集：打开▾/Fit/添加▾/轴/网格/拾取(占位) + 视口角标悬浮层（网格/轴/拾取开关入角标）——确认？
- D4 组级默认值与现有按对象属性的关系：组默认仅作用于"新建成员"还是允许"批量应用到组内现有"？
  （推荐：默认值仅新建成员；批量应用 007 再做）
